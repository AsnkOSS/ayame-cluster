apiVersion: v1
kind: Service
metadata:
  name: nixcn-cms-prod-client-service
  namespace: nixcn-cms-prod
spec:
  selector:
    app: nixcn-cms-prod-client
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
