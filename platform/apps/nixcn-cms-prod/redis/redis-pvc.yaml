apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nixcn-cms-prod-redis-data
  namespace: nixcn-cms-prod
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: hcloud-volumes
  resources:
    requests:
      storage: 10Gi
