apiVersion: v1
kind: Service
metadata:
  name: gitea-redis
  namespace: gitea
spec:
  selector:
    app: gitea-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
