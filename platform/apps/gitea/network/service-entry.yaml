apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: s3-external
  namespace: gitea
spec:
  hosts:
    - fsn1.your-objectstorage.com
  ports:
    - number: 443
      name: https
      protocol: HTTPS
  location: MESH_EXTERNAL
  resolution: DNS
