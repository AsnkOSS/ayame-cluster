apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitea-meilisearch
  namespace: gitea
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: gitea-meilisearch
  template:
    metadata:
      labels:
        app: gitea-meilisearch
    spec:
      containers:
        - name: gitea-meilisearch
          image: getmeili/meilisearch:v1.32
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 7700
              name: http
          env:
            - name: MEILI_ENV
              value: "production"
            - name: MEILI_NO_ANALYTICS
              value: "true"
          volumeMounts:
            - name: meili-data
              mountPath: /meili_data
          readinessProbe:
            httpGet:
              path: /health
              port: 7700
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 7700
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: meili-data
          persistentVolumeClaim:
            claimName: gitea-meilisearch-data
